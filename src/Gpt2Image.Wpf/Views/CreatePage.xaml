<UserControl x:Class="Gpt2Image.Wpf.Views.CreatePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Gpt2Image.Wpf.Converters">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:Base64ImageSourceConverter x:Key="Base64ImageSourceConverter" />
        <Style x:Key="PreviewActionButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="FontSize" Value="12" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" MinHeight="360" />
            <RowDefinition Height="190" MinHeight="150" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,18">
            <StackPanel>
                <TextBlock Text="创作" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="{Binding Status}" Style="{StaticResource StatusTextBlockStyle}" Margin="0,5,24,0" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" MinWidth="300" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" MinWidth="540" />
            </Grid.ColumnDefinitions>

            <Border BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" Background="{StaticResource PanelBrush}" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Margin="0,0,0,14">
                        <TextBlock Text="参数" FontSize="18" FontWeight="SemiBold" />
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="0,0,8,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="提示词" FontWeight="SemiBold" VerticalAlignment="Center" />
                                <Button Grid.Column="1"
                                        Content="优化提示词"
                                        Command="{Binding OptimizePromptCommand}"
                                        MinWidth="96"
                                        Padding="10,4" />
                            </Grid>
                            <TextBox Text="{Binding Prompt, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     MinHeight="170"
                                     Margin="0,7,0,8" />
                            <ProgressBar Height="3"
                                         IsIndeterminate="True"
                                         Visibility="{Binding IsOptimizingPrompt, Converter={StaticResource BoolToVisibilityConverter}}"
                                         Margin="0,0,0,14" />

                            <Border BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" Background="#F8FAFC" Padding="10" Margin="0,0,0,14">
                                <StackPanel>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="参考图 / 编辑图" FontWeight="SemiBold" />
                                            <TextBlock Text="上传后将使用图片编辑/参考图模式，需后端支持图片编辑。" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" TextWrapping="Wrap" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <Button Content="添加图片" Command="{Binding AddInputImagesCommand}" Padding="8,4" Margin="0,0,6,0" />
                                            <Button Content="清空" Command="{Binding ClearInputImagesCommand}" Padding="8,4" />
                                        </StackPanel>
                                    </Grid>

                                    <ItemsControl ItemsSource="{Binding InputImages}" Margin="0,0,0,8">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Width="132" BorderBrush="#E2E8F0" BorderThickness="1" Background="#FFFFFF" Padding="6" Margin="0,0,8,8">
                                                    <StackPanel>
                                                        <Image Source="{Binding PreviewSource, Converter={StaticResource Base64ImageSourceConverter}}" Height="76" Stretch="Uniform" />
                                                        <TextBlock Text="{Binding FileName}" FontSize="12" TextTrimming="CharacterEllipsis" Margin="0,5,0,2" />
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Text="{Binding SizeText}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" />
                                                            <Button Grid.Column="1"
                                                                    Content="删除"
                                                                    Command="{Binding DataContext.RemoveInputImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                    CommandParameter="{Binding}"
                                                                    Padding="5,2"
                                                                    FontSize="11" />
                                                        </Grid>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="蒙版（可选）" FontWeight="SemiBold" />
                                            <TextBlock Text="{Binding MaskImage.FileName, TargetNullValue=未选择蒙版}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" TextWrapping="Wrap" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <Button Content="选择蒙版" Command="{Binding SelectMaskImageCommand}" Padding="8,4" Margin="0,0,6,0" />
                                            <Button Content="清除" Command="{Binding ClearMaskImageCommand}" Padding="8,4" />
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="尺寸" FontWeight="SemiBold" />
                                    <ComboBox ItemsSource="{Binding Sizes}" SelectedItem="{Binding Size}" Margin="0,7,0,14" />
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="数量" FontWeight="SemiBold" />
                                    <TextBox Text="{Binding Count, UpdateSourceTrigger=PropertyChanged}" Margin="0,7,0,14" />
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="质量" FontWeight="SemiBold" />
                            <ComboBox ItemsSource="{Binding Qualities}" SelectedItem="{Binding Quality}" Margin="0,7,0,14" />

                            <TextBlock Text="返回格式" FontWeight="SemiBold" />
                            <ComboBox ItemsSource="{Binding ResponseFormats}" SelectedItem="{Binding ResponseFormat}" Margin="0,7,0,14" />
                        </StackPanel>
                    </ScrollViewer>

                    <StackPanel Grid.Row="2" Margin="0,16,0,0">
                        <ProgressBar Height="4" IsIndeterminate="True" Visibility="{Binding IsGenerating, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,0,0,12" />
                        <Button Content="开始生成" Command="{Binding GenerateCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="2" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" Background="{StaticResource PanelBrush}" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="预览" FontSize="18" FontWeight="SemiBold" />
                    </Grid>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding PreviewImages}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel ItemWidth="430" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="420" Height="540" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" Background="#F8FAFC" Margin="0,0,14,14">
                                        <Border.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="保存到..." Command="{Binding SaveAsCommand}" />
                                                <MenuItem Header="打开文件" Command="{Binding OpenFileCommand}" />
                                                <MenuItem Header="打开文件夹" Command="{Binding OpenFolderCommand}" />
                                                <MenuItem Header="复制路径" Command="{Binding CopyPathCommand}" />
                                            </ContextMenu>
                                        </Border.ContextMenu>
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="390" />
                                                <RowDefinition Height="*" />
                                            </Grid.RowDefinitions>

                                            <Grid Background="#EEF2F7">
                                                <Image x:Name="PreviewImage"
                                                       Source="{Binding PreviewImageSource, Converter={StaticResource Base64ImageSourceConverter}}"
                                                       Stretch="Uniform"
                                                       Visibility="Collapsed" />

                                                <StackPanel x:Name="LoadingPanel" VerticalAlignment="Center" Margin="18">
                                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,10" />
                                                    <ProgressBar IsIndeterminate="True" Height="4" Width="150" />
                                                </StackPanel>

                                                <Border x:Name="ErrorPanel" Background="#FEF2F2" Visibility="Collapsed" Padding="14">
                                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                                        <TextBlock Text="{Binding Status}" Foreground="#991B1B" TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Center" />
                                                    </ScrollViewer>
                                                </Border>
                                            </Grid>

                                            <Border Grid.Row="1" Background="#FFFFFF" Padding="10">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto" />
                                                        <RowDefinition Height="*" />
                                                        <RowDefinition Height="Auto" />
                                                    </Grid.RowDefinitions>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                                        <TextBlock Grid.Column="1" Text="{Binding FileName}" Foreground="{StaticResource TextSecondaryBrush}" MaxWidth="190" TextTrimming="CharacterEllipsis" />
                                                    </Grid>
                                                    <TextBlock Grid.Row="1" Text="{Binding Status}" Foreground="#475569" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" Margin="0,5,0,0" />
                                                    <UniformGrid Grid.Row="2" Columns="4" Margin="0,10,0,0">
                                                        <Button Content="另存为" Command="{Binding SaveAsCommand}" Style="{StaticResource PreviewActionButtonStyle}" Margin="0,0,6,0" />
                                                        <Button Content="打开" Command="{Binding OpenFileCommand}" Style="{StaticResource PreviewActionButtonStyle}" Margin="0,0,6,0" />
                                                        <Button Content="文件夹" Command="{Binding OpenFolderCommand}" Style="{StaticResource PreviewActionButtonStyle}" Margin="0,0,6,0" />
                                                        <Button Content="复制路径" Command="{Binding CopyPathCommand}" Style="{StaticResource PreviewActionButtonStyle}" />
                                                    </UniformGrid>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSaved}" Value="True">
                                                        <Setter Property="BorderBrush" Value="#16A34A" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding HasError}" Value="True">
                                                        <Setter Property="BorderBrush" Value="#DC2626" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding HasImage}" Value="True">
                                            <Setter TargetName="PreviewImage" Property="Visibility" Value="Visible" />
                                            <Setter TargetName="LoadingPanel" Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding HasError}" Value="True">
                                            <Setter TargetName="LoadingPanel" Property="Visibility" Value="Collapsed" />
                                            <Setter TargetName="ErrorPanel" Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" Background="{StaticResource PanelBrush}" Padding="14" Margin="0,18,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="日志" FontSize="16" FontWeight="SemiBold" />
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding RunLogs}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#EDF0F4" BorderThickness="0,0,0,1" Padding="0,0,0,7" Margin="0,0,0,7">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="76" />
                                            <ColumnDefinition Width="56" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding TimeText}" Foreground="{StaticResource TextSecondaryBrush}" />
                                        <TextBlock Grid.Column="1" Text="{Binding Level}" FontWeight="SemiBold" Foreground="#334155" />
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="{Binding Message}" Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap" />
                                            <TextBlock Text="{Binding Detail}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,2,0,0" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
